#pragma once
#include <rho.h>
#include "coreApi.h"
namespace rho
{
    CORE_API string to_string(ivec2 value);

    CORE_API string to_string(vec2 value);

    CORE_API string to_string(vec3 value);

    CORE_API string to_string(vec4 value);
}
